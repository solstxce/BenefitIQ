[project]
name = "benefitiq-backend"
version = "1.0.0"
description = "BenefitIQ backend - FastAPI benefit intelligence platform"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "sqlalchemy>=2.0",
    "pydantic>=2.0",
]

[tool.uv]
package = false
